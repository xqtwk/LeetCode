package jv;

public class BoatsToSavePeople {
    /*
    public int numRescueBoats(int[] people, int limit) {
        ArrayList<Integer> p = Arrays.asList(people);
    }
        for (int i = 0; i < people.length; i++) {
        }
    */
}
